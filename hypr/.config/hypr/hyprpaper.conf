# 1. Preload the image (this stays the same)
preload = ~/hyprdotfiles/wallpapers/cody.png

# 2. Use the new block syntax for the wallpaper target
wallpaper {
    monitor = eDP-1
    path = ~/hyprdotfiles/wallpapers/cody.png
}

# 3. Disable the splash text
splash = false
